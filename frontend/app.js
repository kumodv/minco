import sidebar from './srcs/components/sidebar.js';

document.addEventListener('DOMContentLoaded', function() {
    // changeUrl(window.location.pathname);
    sidebar();
    // createFriends();
});